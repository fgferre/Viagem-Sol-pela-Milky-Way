# BACKLOG — noticed, not done (max 30 lines, one per line)

- Unused-code check: `npx knip` on 2026-09-04 found 120 unused exports, 11 unused types, 1 duplicate export; its 20 "unused files" are hand-run CLI scripts, so a knip config needs `entry: ['scripts/**/*.mjs']` before it can join `npm run done`.
- `src/components/FichaDoObjeto.tsx` useMemo lists `idioma` as an unnecessary dependency (lint warning).
- The six AI-illustrated maps (Hígia, Palas, Haumea, Makemake, Éris, Quaoar) enter `public/data/atlas/texturas.json` as proveniencia `derivado`, so the ficha's seal calls an invented image "derivado"; owner's call whether it should read `artistico`.
- `npm test` needs a Chrome binary because `scripts/visual/chrome.mjs` resolves it at import time; a lazy resolve would let the suite run anywhere and retire that trap from AGENTS.md.
- `docs/RENDERER_CARTOGRAPHY.md` predates the 17/08 architecture wave; a 2026-09-04 spot check (5 paths, 3 mechanisms) still matched the code, so a full truth pass is the remaining doubt.
- `docs/PENDENCIAS-ARQUIVO.md` (567 KB) duplicates what git history holds; owner decides whether it goes.
- `src/three/world/corpos/rochoso.ts`: the procedural-surface shader branch (`superficie: 'procedural'`) has no consumer since item 151; remove it with its test (owner approves).
- The public-data hook (`.claude/hooks/bloqueia-public-data.sh`) blocks read-only commands (`git diff`, `sed -n`, even a heredoc) that merely mention an atlas data-script path; it should key on the command verb, not on the path token.
- Fluidez em DPR 2 (medido 05/09 em tela REAL, Chrome visível, 2560×1266, filme em t=100): quadro parado 50 ms, em movimento 83 ms. Desligar os DOIS blooms rende só 2,5 fps parado e 0,4 em movimento; `?knee=0` rende zero. O borrão pareado por bilinear (itens 3 e 4 da análise do ChatGPT) foi construído e descartado: 18,5→18,7 fps, e muda ±3/255 em 70% dos pixels (`capturas/fluidez3-blur-*.png`). O que pesa é o raymarch da nebulosa em MOVIMENTO (~33 ms, é o que o cache parado economiza) e a própria cena (~50 ms; `cena:heroStars` no topo do timer query). Oclusão por planetas também morreu: perto deles a nebulosa já está desligada pela rampa `nebulaFade` (`capturas/fluidez2-*.png`).
- Régua de fluidez: `gpu-profile.mjs` por timer query dá 350–430 ms/quadro a 30 fps em DPR 2 (fora do regime, draws sobrepostos no ANGLE/Metal), e headless sem vsync desenha a 200+ fps porque não apresenta. A única régua que reproduziu a experiência do dono foi Chrome VISÍVEL (GPU_FLAGS sem `--headless`, `--force-device-scale-factor=2`, janela 1280×720), esperar `captura.pronto` e contar rAF por 6 s: repetível a ±0,2 fps. Dono decide se vira um modo do `gpu-profile`.
- Camada da GALÁXIA dentro do disco (medido 05/09 em tela real, DPR 2): em t=140–160 do filme o quadro custa ~87–95 ms e cai para ~13–25 ms com `?nogal=1`; `?nocart=1` não muda nada, `galstat` mostra só 1,8% dos 4 M pontos no frustum em t=140 — o peso deve ser o TAMANHO dos pontos/discos de perto (fill aditivo), não a contagem. `update()` da galáxia reescreve `visible` dos objetos a cada quadro, então esconder objeto de fora não mede nada: esconder por MATERIAL (`brightMat.visible`, `discMats[i].visible`) é o jeito de repartir pontos × discos × brilhos. Maior ganho de fluidez que resta na viagem; item próprio, decisão do dono.
- Medição térmica: depois de ~2 h de medições seguidas o M1 estrangula e a mesma vista sobe de 87 para 117 ms. Intercalar descanso ou medir em rodadas curtas; comparar sempre com uma base medida no mesmo minuto.
- Partículas da galáxia perto da câmera (`unresolved(dist)` ≈ 1 − Σamp) rasterizam até 23 px cada com alpha quase zero; um corte exato (`vAlpha <= 0.0`) provavelmente nunca dispara porque Σamp < 1 — só um limiar (decisão de régua) economizaria; medir antes de propor.
- ARMADILHA DA RÉGUA (05/09): medir ENGASGOS sem vsync (`--disable-gpu-vsync`) é inválido — o processador enfileira quadros muito à frente da placa e qualquer ponto de sincronia (link de shader, readPixels, upload de textura) espera a fila inteira: mediu-se 3–6 s por link, 2,4 s num readPixels de 32 KB e 4 s num texSubImage2D. Com vsync LIGADO, na chegada do filme (t=178) o quadro máximo é 84–151 ms, sem nenhum quadro >250 ms, antes E depois de aquecer Terra/Lua/CME (três commits construídos sobre a medida falsa e revertidos no mesmo dia). Regra: sem vsync só para TEMPO DE QUADRO (bate com o modo vsync); engasgos, só com vsync. Fica a hipótese, não medida, de que em placas bem mais lentas o aquecimento dos corpos ajude. O engasgo de 4,4 s em t≈15 da abertura também foi visto só sem vsync: remedir com vsync antes de investigar.
- Juiz `a11y.mjs`: as duas checagens "alças (390×844 e 320×568, ui = 1.4): 5 na fileira" falharam UMA vez (06/09, logo após `npm test` com a máquina carregada) e não reproduziram em duas corridas seguidas, com e sem a mudança do painel — provável `dorme(200)` curto depois de trocar a escala `ui` antes de ler `.atlas-alcas`; trocar por `esperarPor`. A falha "o nome na ficha MUDA com o foco" é anterior e constante.
